version: "3.7"

services:
  pg:
    image: bitnami/postgresql:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=shopper
      - POSTGRES_PASSWORD=shopper
      - POSTGRES_DB=shopper
